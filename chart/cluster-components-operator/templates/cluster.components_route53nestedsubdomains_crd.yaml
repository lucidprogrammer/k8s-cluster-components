apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  labels:
    app: cluster-components-operator
    chart: '{{ include "cluster-components-operator.chart" . }}'
    heritage: Helm
    release: '{{ .Release.Name }}'
  name: route53nestedsubdomains.cluster.components
spec:
  group: cluster.components
  names:
    kind: Route53NestedSubdomain
    listKind: Route53NestedSubdomainList
    plural: route53nestedsubdomains
    singular: route53nestedsubdomain
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        type: object
        x-kubernetes-preserve-unknown-fields: true
    served: true
    storage: true
    subresources:
      status: {}

---